'use client'

/**
 * Template Styles (Simplificado)
 * 
 * Este componente foi simplificado. Os estilos do template agora são carregados
 * diretamente no iframe através do sistema de isolamento (isolated-preview-styles.tsx).
 * 
 * Este componente não aplica mais estilos no documento principal do admin para evitar
 * interferência. Todo o isolamento é feito dentro do iframe.
 */

export function TemplateStyles() {
  // Componente vazio - isolamento agora é feito no iframe
  return null
}

