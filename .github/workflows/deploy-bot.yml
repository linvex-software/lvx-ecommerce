name: Deploy

# Deploy após merge na main (arquitetura correta)
# O GitHub faz o merge automaticamente, não precisamos fazer merge manual
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Deploy
        run: echo "Deploy aqui - configure seu processo de deploy"